[package]
name = "terminal"
version = "0.1.0"
edition = "2024"

[dependencies]
elibc = { path = "../elibc"}
spin = "0.10.0"
noto-sans-mono-bitmap = { version = "0.3.1", features = [
    "bold",
    "size_24",
    "size_32",
] }
