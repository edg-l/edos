
- dns resolver: `dns example.com` resolves, `dns edgl.dev` says "No A record
  found" though the domain has A records (both are Cloudflare-fronted, so it is
  not CNAME flattening). Suspect answer-section parsing.

- add a way to kill threads from user (user threads, maybe signals)

- research what try_clone on pipe means, can pipes be cloned?

- add system time (real time) as opposed to just time since boot

- seek and related syscalls

- users, file perms

- set file time syscall?

- direct copy and rename file

- std fork: OpenOptions read/write/truncate/create_new were no-op stubs, so
  every file opened read-only from the kernel's point of view and
  mmap(MAP_SHARED, PROT_WRITE) was refused with EACCES. FIXED locally in
  ~/dev/rust (library/std/src/sys/fs/edos.rs): the access mode now goes in
  the low two bits and truncate sets 0x200. mmaptest goes from failing at
  test 3 to all 10 passing on both /var and /tmp. NOT YET PUSHED to the
  fork, and edos_rt still lacks RDONLY/WRONLY/RDWR/TRUNCATE constants, so
  std spells the values out itself.

- std fork: decode_error_kind (sys/pal/edos/common.rs) maps only EACCES,
  EEXIST, EINVAL, ENOENT, EPERM. Everything else shows as "uncategorized
  error". Worth covering ENOTDIR, EISDIR, EIO, ENOSPC, EROFS, EPIPE,
  EAGAIN, EINTR at least.
